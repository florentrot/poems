import { ReverseListPipe } from './reverse-list.pipe';

describe('ReverseListPipe', () => {
  it('create an instance', () => {
    const pipe = new ReverseListPipe();
    expect(pipe).toBeTruthy();
  });
});
